'use client';

import Sidebar from '@/components/sidebar';
import React, { useState } from 'react';

const Bookings = () => {
	return (
		<Sidebar>
			<div>Booking</div>
		</Sidebar>
	);
};

export default Bookings;
